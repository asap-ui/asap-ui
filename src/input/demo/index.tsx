import { AppBar, Input } from 'asap-ui';
import React from 'react';

const InputDemo = () => {
  return (
    <div>
      <AppBar title="基本使用">
        <Input placeholder={'22233'} />
      </AppBar>
    </div>
  );
};

export default InputDemo;
